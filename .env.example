# Toggle the mock data layer. Set to "false" once the backend swagger lands.
NEXT_PUBLIC_USE_MOCK_API=true

# Base URL of the backend API (ignored while the mock layer is on).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api
